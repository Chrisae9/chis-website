/**
 * Site-wide constants and configuration values
 * These values are used throughout the application
 */

/**
 * Application name used for metadata, titles, etc.
 */
export const APP_NAME = 'Blog';

/**
 * Base URL of the site for canonical links and absolute URLs
 */
export const SITE_URL = 'https://chis.dev';

/**
 * Default meta description for SEO
 */
export const DEFAULT_DESCRIPTION = 'Personal blog and knowledge base';

/**
 * Maximum width for content containers (in pixels)
 * Used for consistent layout sizing
 */
export const MAX_CONTENT_WIDTH = 768;

/**
 * Large breakpoint width for responsive design (in pixels)
 * This matches the Tailwind 'lg' breakpoint
 */
export const BREAKPOINT_LARGE = 1024;