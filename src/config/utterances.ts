/**
 * Configuration for Utterances comments integration
 * Utterances is a lightweight comments widget built on GitHub issues
 * @see https://utteranc.es/
 */
export const utterancesConfig = {
  /** GitHub repository in format 'owner/repo' where comments are stored as issues */
  repo: 'Chrisae9/chis-website',
  /** Theme configuration for light/dark mode support */
  theme: {
    /** Theme name for light mode */
    light: 'github-light',
    /** Theme name for dark mode */
    dark: 'github-dark'
  }
};
